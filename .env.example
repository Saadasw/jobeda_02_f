# Base URL the SPA calls. In dev this is "/api", which Vite proxies to the
# FastAPI backend (see vite.config.ts) so cookies stay first-party.
# In production, point this at the deployed API origin or a same-origin proxy path.
VITE_API_BASE_URL=/api
